{
 "change_id": 104135,
 "provider": "vercel",
 "source": {
  "kind": "spec-diff",
  "anchor": "/v1/edge-config/{edgeConfigId}/token/{token}",
  "detail": "vercel OAS corridor pair v1.28.13 -> v1.28.14 (removals subset ingested 2026-08-01)"
 },
 "classifier": "llm-fix-v1",
 "generated_at": "2026-08-01T12:00:00.000Z",
 "verdict": "code-fixable",
 "confidence": "high",
 "spec_evidence": {
  "old_version": "vercel vercel-spec.json @ v1.28.13",
  "new_version": "vercel vercel-spec.json @ v1.28.14",
  "checked": "Walked both cached specs: OLD paths contains /v1/edge-config/{edgeConfigId}/token/{token} while NEW does not (path removed); NEW paths contains /v1/global-config/{edgeConfigId}/token/{token} which is absent from OLD, and the successor path carries the identical HTTP method set (get) as the removed path did in OLD \u2014 a one-to-one resource-family rename (edge-config -> global-config) within the v1.28.13 -> v1.28.14 corridor."
 },
 "alternative_data_source": "the same resource is served at /v1/global-config/{edgeConfigId}/token/{token} in the NEW spec with an identical method set",
 "fix": {
  "strategy": "endpoint-path-rename",
  "migration_guide": "Mechanical path rewrite at every call site: replace the '/edge-config' segment with '/global-config' in raw URL builders, route constants, and API client wrappers hitting '/v1/edge-config/{edgeConfigId}/token/{token}'. The successor route exposes the same method set (get) with the same path parameters, so no request or response handling edits are required beyond the one-segment rename.",
  "example_before": "const res = await fetch(`https://api.vercel.com/v1/edge-config/${id}/token/${token}`, opts);",
  "example_after": "const res = await fetch(`https://api.vercel.com/v1/global-config/${id}/token/${token}`, opts);"
 }
}
